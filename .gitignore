node_modules/
.DS_Store
*.local

# Android build output — regenerated by Gradle / Android Studio, safe to ignore.
# NOTE: the android/ folder itself is now tracked so your native code
# (MainActivity.java NativePlugin, manifest, gradle, res) is backed up in git.
android/build/
android/app/build/
android/.gradle/
android/.idea/
android/local.properties
android/app/release/
android/capacitor-cordova-android-plugins/build/
*.apk
*.aab
